rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the server (firebase-admin).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
